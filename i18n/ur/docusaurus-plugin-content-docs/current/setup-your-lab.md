---
sidebar_position: 2
title: "🛠️ اپنی لیب سیٹ اپ کریں"
description: "فزیکل اے آئی ڈویلپمنٹ ماحول تیار کرنے کے لیے مرحلہ وار گائیڈ۔"
---

# اپنی لیب سیٹ اپ کریں: ماحول کی تیاری

ہیومنائیڈ روبوٹس بنانے سے پہلے، ہمیں یہ یقینی بنانا ہوگا کہ آپ کے پاس صحیح اوزار موجود ہیں۔ اپنی فزیکل اے آئی لیب تیار کرنے کے لیے اس گائیڈ پر عمل کریں۔

---

## 1. سسٹم کی ضروریات
- **آپریٹنگ سسٹم**: Ubuntu 22.04 LTS (تجویز کردہ) یا Windows 11 مع WSL2۔
- **جی پی یو (GPU)**: NVIDIA RTX 3060 یا اس سے بہتر (NVIDIA Isaac Sim کے لیے ضروری)۔
- **ریم (RAM)**: کم از کم 16 جی بی (32 جی بی بہتر ہے)۔
- **ڈسک اسپیس**: 100 جی بی خالی جگہ۔

---

## 2. پروگرامنگ ٹولز

### Node.js انسٹال کریں
ہم اے آئی بینک اینڈ اور ڈاکوسورس فرنٹ اینڈ کے لیے Node.js استعمال کرتے ہیں۔
```bash
# NVM کا استعمال کریں (تجویز کردہ)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.5/install.sh | bash
nvm install 20
```

### Python انسٹال کریں
روبوٹکس اور اے آئی کے لیے ازگر (Python) بنیادی زبان ہے۔
```bash
sudo apt update
sudo apt install python3-pip python3-dev
```

---

## 3. روبوٹکس فریم ورکس

### ROS 2 (Humble Hawksbill) انسٹال کریں
ROS 2 ہمارے روبوٹ کا "اعصابی نظام" ہے۔
```bash
# اپنے سسٹم کے لیے آفیشل ROS 2 Humble انسٹالیشن گائیڈ دیکھیں
# لنک: https://docs.ros.org/en/humble/Installation.html
```

### Gazebo Sim انسٹال کریں
گیزیبو (Gazebo) ہمارا بنیادی فزکس سیمولیٹر ہے۔
```bash
sudo apt-get update
sudo apt-get install ros-humble-ros-gz
```

---

## 4. NVIDIA Isaac Sim
جدید فوٹو ریئلسٹک سیمولیشن اور ڈیٹا جنریشن کے لیے۔
1. **NVIDIA Omniverse Launcher** ڈاؤن لوڈ کریں۔
2. ایکسچینج ٹیب سے **Isaac Sim** انسٹال کریں۔
3. **Isaac ROS** ورک اسپیس کی ترتیب بنائیں۔

---

## 5. پروجیکٹ سیٹ اپ

ٹیکسٹ بک ریپوزٹری کلون کریں اور ضروری فائلز انسٹال کریں:

```bash
git clone https://github.com/panaversity/physical-ai-textbook.git
cd physical-ai-textbook

# فرنٹ اینڈ انسٹال کریں
npm install

# بیک اینڈ سیٹ اپ
cd backend
npm install
cp .env.example .env
```

---

## 6. تصدیق: ٹیسٹ رن
یہ یقینی بنانے کے لیے کہ سب کچھ ٹھیک کام کر رہا ہے، درج ذیل کمانڈز چلائیں:

### اے آئی بیک اینڈ شروع کریں
```bash
node backend/server.js
```

### ٹیکسٹ بک سائٹ شروع کریں
```bash
npm start
```

---

## 7. چیک لسٹ
- [ ] `node -v` کا رزلٹ v20 یا اس سے اوپر ہے۔
- [ ] `ros2 doctor` تمام ٹیسٹ پاس کر چکا ہے۔
- [ ] NVIDIA ڈرائیورز اپ ٹو ڈیٹ ہیں (`nvidia-smi`)۔

**مبارک ہو! آپ کی لیب اب تیار ہے۔ اب فزیکل اے آئی بنانا شروع کرتے ہیں۔**
